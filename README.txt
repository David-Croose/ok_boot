19:59 2017/12/9
用Qt做一个上位机，使用串口跟下位机通信，上位机可以读取bin文件，并把bin文件传输到下位机中；
还可以随时控制下位机执行boot或者app

首先用战舰开发板来做，因为它编程比较快，把它调通了再在SAMA5D3上做。注意两块开发板的运行过程有点不同，战舰开发板是有flash的，bin文件传输一次成功，以后就可以完全不用上位机了；但是SAMA5D3不同，它只有片上RAM，每次启动后执行boot，但boot不能从flash中获取bin文件，只能从上位机获取